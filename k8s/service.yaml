apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  namespace: rag-production
spec:
  type: ClusterIP

  selector:
    app: rag-api

  ports:
    - name: http
      port: 80
      targetPort: http